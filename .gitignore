# macOS
.DS_Store

# Swift Package Manager / Xcode
.build/
.swiftpm/
xcuserdata/
DerivedData/
*.xcuserstate
macos/RecordingAgent/.build/

# Swift MVP build outputs(归档到 其他文件/build/,不进 Git)
*.app/
*.dmg

# Local-only workspace(AGENTS.md / AI文档/ / 其他文件/ 是项目本地-only 区,见 AGENTS.md §2)
AGENTS.md
AI文档/
.claude/
.codex/
.agents/
.context/
其他文件/

# API key / secret 防护:禁止将明文密钥提交到仓库
# 凡是匹配常见 API key 模式的文件,必须使用环境变量或系统 Keychain
*.key
*.secret
*key*.md
*secret*.md
*api*key*.md

# Local runtime data(永不进 Git)
*.sqlite
*.sqlite-*
*.db
recordings/

# Model and sidecar binaries
*.gguf
*.safetensors
*.onnx
argmax-cli
llama-cli
llama-completion
