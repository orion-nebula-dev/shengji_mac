# v0.4 架构地基实施记录

文档状态：已完成
适用分支：`feature-v0.4.0-architecture-foundation`
首次记录日期：2026-06-13

## 1. 本 plan 目标

本 plan 只交付 v0.4 的架构地基，不接入真实 WhisperKit、SpeakerKit 或 MiniMax M3 生成链路。

已落地范围：

1. 新增 Rust provider catalog 与 provider trait 边界。
2. 新增独立 provider 配置字段：
   - `asr_provider_type`
   - `speaker_provider_type`
   - `semantic_provider_type`
   - `embedding_provider_type`
   - `export_provider_type`
3. 新增 MiniMax M3 语义配置字段：
   - `semantic_base_url`
   - `semantic_model_name`
   - `semantic_api_key_ref`
4. 新增统一 AI 产物与模型调用记录表：
   - `semantic_artifacts`
   - `model_invocations`
5. Todo 入口固定为 `semantic_m3`，只登记 `semantic_artifacts(type='todo_extraction')` 边界，不再保留旧本地 LLM 或旧直连云端 Todo 路径。
6. 旧 Qwen / llama.cpp Todo runtime、内嵌资源、CLI 子命令和前端入口已移除。
7. 设置页展示本地 ASR、本地说话人分离、云端 MiniMax M3、Embedding 预留和本地导出的隐私边界。
8. 保存 image2 生成的 Apple 风格视觉参考图。

## 2. Provider 边界

| Provider | 能力 | 默认 ID | 隐私边界 |
| --- | --- | --- | --- |
| Local WhisperKit / Argmax | ASR | `local_whisperkit` | 音频默认留在本机，v0.5 接入实际 local server |
| Local SpeakerKit | Speaker | `local_speakerkit` | 本地生成 speaker label 与时间段，不识别真实姓名 |
| MiniMax M3 | Semantic | `minimax_m3` | 只发送转写后的文本上下文，用于摘要、Todo、脑图和研究 |
| Reserved Embedding | Embedding | `reserved` | v0.4 不启用默认路径 |
| Local File Export | Export | `local_file` | 导出默认写入用户选择的本地位置 |

Todo 相关说明：

1. `todo_provider_type` 默认值固定为 `semantic_m3`。
2. v0.4 处理 Todo job 时只登记 Todo 语义产物边界，实际 Todo 候选生成进入后续版本。
3. 旧 `embedded_local`、`legacy_local_llm`、`cloud` Todo provider 路径不再保留运行时兼容入口，Todo provider 固定为 `semantic_m3`。

## 3. 新增表

### `semantic_artifacts`

用于统一承载摘要、Todo 候选、脑图、Moment、深度研究、翻译等 AI 产物。

关键字段：

1. `session_id`
2. `artifact_type`
3. `status`
4. `provider`
5. `model_name`
6. `schema_version`
7. `source_span_refs`
8. `payload_json`
9. `error_message`

### `model_invocations`

用于记录模型调用摘要，不记录完整隐私文本和完整密钥。

关键字段：

1. `provider`
2. `model_name`
3. `capability`
4. `status`
5. `request_summary`
6. `response_summary`
7. `input_tokens`
8. `output_tokens`
9. `duration_ms`
10. `estimated_cost_microunits`
11. `currency`
12. `error_message`
13. `trace_id`
14. `started_at`
15. `finished_at`

## 4. 非目标

本 plan 不做以下内容：

1. 不启动 Argmax / WhisperKit sidecar。
2. 不执行真实 speaker diarization。
3. 不调用 MiniMax M3。
4. 不生成摘要、脑图或深度研究产物。
5. 不完成 `src-tauri/src/lib.rs` 的全量物理拆分。

这些内容继续按 `AI文档/03-版本迭代/声记-版本迭代目标与代码归档方案.md` 进入后续 plan。

## 5. Plan 2：旧本地 LLM Provider 迁移

2026-06-13 完成第二个小步迁移：

1. 新增 `src-tauri/src/providers/semantic/legacy_local_llm.rs`，承载旧 Qwen / llama.cpp Todo 运行时逻辑。
2. `src-tauri/src/lib.rs` 不再保留本地 LLM 的 Prompt、manifest、JSON 解析、子进程请求等实现细节，只通过 `legacy_local_llm` provider 边界调用。
3. 单元测试改为直接验证 provider 边界：
   - 旧 `todo-embedded-v1` 版本归一化到 `qwen3-4b-instruct-2507-q4_k_m`
   - manifest 路径稳定
   - Prompt 注入、manifest 解析、运行时 Todo JSON 解析仍保持原行为
4. 默认 Todo 主线仍为 `semantic_m3`，`legacy_local_llm` 仅作为显式选择的旧路径保留。
5. Tauri 启动和 CLI job 入口不再无条件释放 legacy runtime 文件，只有设置为 `legacy_local_llm` 时才准备旧本地运行时资源。

后续 Plan 6 已按“无老用户”前提删除该 legacy 路径，本节仅保留历史实施记录。

## 6. Plan 3：主线 Provider 模块契约

2026-06-13 完成第三个小步迁移：

1. 新增 `src-tauri/src/providers/asr/local_whisperkit.rs`，定义 Local WhisperKit / Argmax 的本地 ASR provider descriptor、OpenAI Audio API 转写 path 和运行模式边界。
2. 新增 `src-tauri/src/providers/speaker/local_speakerkit.rs`，定义 Local SpeakerKit 的本地说话人分离 provider descriptor 和手动修正能力边界。
3. 新增 `src-tauri/src/providers/semantic/minimax_m3.rs`，定义 MiniMax M3 语义 provider descriptor、默认 base URL、默认模型名、上下文上限和支持的 artifact 类型。
4. `provider_catalog()` 改为复用各 provider 模块自己的 `descriptor()`，避免 catalog 与具体 provider 边界分裂。
5. `semantic_artifacts.artifact_type` 提前纳入 `transcript_revision`、`recording_type`、`meeting_minutes`，并为旧 CHECK 约束增加表重建迁移，确保 MiniMax M3 provider 支持列表与持久化 schema 一致。
6. 本 plan 仍不接真实 Argmax 进程、SpeakerKit 推理或 MiniMax M3 网络调用；真实能力进入后续 v0.5 / v0.6。

## 7. Plan 4：SQLite Infra 边界抽取

2026-06-13 完成第四个小步迁移：

1. 新增 `src-tauri/src/infra/sqlite.rs` 与 `src-tauri/src/infra/mod.rs`。
2. 将数据库连接、schema 初始化、设置表迁移、会话表迁移、`model_invocations` 列迁移、`semantic_artifacts` 类型约束表重建迁移和 demo seed 从 `src-tauri/src/lib.rs` 移入 `infra::sqlite`。
3. `src-tauri/src/lib.rs` 通过 `infra::sqlite::{initialize_database, open_connection}` 调用数据库基础设施，不再直接承载 SQLite schema 细节。
4. 新增单元测试验证 `infra::sqlite` 可初始化数据库、打开连接，并创建 `semantic_artifacts` 等 v0.4 基础表。
5. 本 plan 不拆 Tauri commands、录音控制、会话聚合或 Todo 业务服务；这些继续进入后续小步迁移。

## 8. Plan 5：Settings App Service 边界抽取

2026-06-13 完成第五个小步迁移：

1. 新增 `src-tauri/src/app/mod.rs` 与 `src-tauri/src/app/settings_service.rs`。
2. 将设置读取、设置保存和录音开关状态写入从 `src-tauri/src/lib.rs` 移入 `app::settings_service`。
3. `src-tauri/src/lib.rs` 中的 Tauri commands、启动初始化、运行状态和测试只通过 `settings_service` 调用设置业务逻辑。
4. 新增单元测试验证 `app::settings_service` 可读取、保存并回读设置。
5. 本 plan 不拆 `settings_commands.rs`，也不改前端设置页交互；commands 物理拆分进入后续小步。

## 9. Plan 6：移除旧本地 Todo 路径

2026-06-13 完成第六个小步迁移：

1. 删除 `src-tauri/src/providers/semantic/legacy_local_llm.rs` 和 `src-tauri/resources/embedded_models/todo/` 下的旧内嵌模型资源。
2. 删除 `SMART_TODO_EMBEDDED_TODO_RUNTIME` 子进程入口、旧本地 Todo runtime 状态 command、旧 Todo 直连云端分支和相关测试。
3. Todo provider 在运行时固定为 `semantic_m3`，不再保留旧 `embedded_local`、旧本地 LLM 或旧 Todo 云端兼容入口。
4. `app_settings` 新 schema 不再创建旧 Todo runtime 字段和旧 Todo 直连云端配置字段。
5. 前端设置页只展示 MiniMax M3 Todo 语义入口，ASR 云端兜底保留在语音转写设置区。

## 10. Plan 7：Jobs Todo 语义边界抽取

2026-06-13 完成第七个小步迁移：

1. 新增 `src-tauri/src/jobs/mod.rs` 与 `src-tauri/src/jobs/todo_extraction.rs`。
2. 将 Todo 语义产物边界登记、空会话跳过和会话提取状态更新从 `src-tauri/src/lib.rs` 移入 `jobs::todo_extraction`。
3. `src-tauri/src/lib.rs` 中的 pending job 调度只负责查询任务、更新任务状态和调用 jobs service，不再直接承载 Todo 语义产物写入细节。
4. 新增单元测试验证 `jobs::todo_extraction::generate_for_session()` 可登记 `semantic_artifacts(type='todo_extraction')`。
5. 本 plan 不拆转写 job、录音 command 或 Tauri command 物理模块；这些继续进入后续小步迁移。

## 11. Plan 8：Model Test Command 边界抽取

2026-06-13 完成第八个小步迁移：

1. 新增 `src-tauri/src/commands/mod.rs` 与 `src-tauri/src/commands/model_test.rs`。
2. 将 `test_model_connection` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::model_test`。
3. 新增 `test_model_connection_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::model_test::test_model_connection`。
5. 新增单元测试验证 Todo 模型测试入口仍只返回 MiniMax M3 语义边界说明，不发起真实 Todo 生成或旧模型调用。
6. 本 plan 不拆桌面上下文、设置保存、录音或 Todo 状态 command；这些继续进入后续小步迁移。

## 12. Plan 9：Settings Command 边界抽取

2026-06-13 完成第九个小步迁移：

1. 新增 `src-tauri/src/commands/settings.rs`。
2. 将 `save_settings` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::settings`。
3. 新增 `save_settings_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::settings::save_settings`。
5. 新增单元测试验证 settings command 可保存并回读设置，Todo provider 仍固定为 `semantic_m3`。
6. 本 plan 不拆 bootstrap、桌面上下文、录音或 Todo 状态 command；这些继续进入后续小步迁移。

## 13. Plan 10：Desktop Context Command 边界抽取

2026-06-13 完成第十个小步迁移：

1. 新增 `src-tauri/src/commands/desktop_context.rs`。
2. 将 `get_desktop_context` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::desktop_context`。
3. 新增 `build_desktop_context()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::desktop_context::get_desktop_context`。
5. 新增单元测试验证桌面上下文仍展示 Tauri runtime、系统平台、录音状态、provider catalog 数量和 MiniMax M3 Todo 边界文案。
6. 本 plan 不拆 bootstrap、录音或 Todo 状态 command；这些继续进入后续小步迁移。

## 14. Plan 11：旧 Todo Provider 兼容残留清理

2026-06-13 完成第十一个小步迁移：

1. 删除后端 `normalize_todo_provider_type()` 兼容函数，设置读写和 Todo 失败状态记录直接使用 `semantic_m3`。
2. 将缺失设置列迁移测试改为验证 v0.4 默认列补齐，不再构造旧 Todo provider 测试数据。
3. Settings service 与 settings command 测试只验证 MiniMax M3 语义入口固定值，不再模拟旧本地 Todo 或旧 Todo 云端输入。
4. 前端 localStorage 状态恢复删除旧 Todo `cloud` 分支，只保留 `semantic_m3` 作为 Todo provider。
5. 源码扫描确认 `src` / `src-tauri/src` / 配置文件中不再出现旧 Qwen、llama.cpp、旧本地 Todo 或旧 Todo provider 名称；历史规划文档中的归档说明不作为运行时入口。

## 15. Plan 12：Bootstrap Command 边界抽取

2026-06-13 完成第十二个小步迁移：

1. 新增 `src-tauri/src/commands/bootstrap.rs`。
2. 将 `get_bootstrap_data` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::bootstrap`。
3. 新增 `get_bootstrap_data_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::bootstrap::get_bootstrap_data`。
5. 新增单元测试验证 bootstrap command 可读取设置、Todo、会话和 runtime 数据，且 Todo provider 仍固定为 `semantic_m3`、语义 provider 仍为 MiniMax M3。
6. 本 plan 不拆 Todo 状态、pending jobs、录音 command 或底层 query service；这些继续进入后续小步迁移。

## 16. Plan 13：Todo Command 边界抽取

2026-06-13 完成第十三个小步迁移：

1. 新增 `src-tauri/src/commands/todo.rs`。
2. 将 `toggle_todo_status` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::todo`。
3. 新增 `toggle_todo_status_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::todo::toggle_todo_status`。
5. 新增单元测试验证 Todo 状态可从 `pending` 切到 `completed`，再次调用可回到 `pending`。
6. 本 plan 不拆 Todo 查询 service、pending jobs、手动 flush 或录音 command；这些继续进入后续小步迁移。

## 17. Plan 14：Pending Jobs Command 边界抽取

2026-06-13 完成第十四个小步迁移：

1. 新增 `src-tauri/src/commands/jobs.rs`。
2. 将 `process_pending_jobs` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::jobs`。
3. 新增 `process_pending_jobs_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::jobs::process_pending_jobs`。
5. 新增单元测试验证 pending jobs command 在无待处理任务时仍返回 runtime、latest session、Todo 列表和 session 列表。
6. 本 plan 不拆 pending job runner 内部、手动 flush 或录音 command；这些继续进入后续小步迁移。

## 18. Plan 15：MiniMax M3 Provider 后端收敛

2026-06-13 根据 command 边界抽取后的 subagent code review 反馈，完成模型边界修复：

1. `settings_service::load_settings()` 不再信任数据库中的非 MiniMax M3 语义 provider 值，统一返回 `minimax_m3`。
2. `settings_service::save_settings()` 不再持久化外部 command payload 中的非 MiniMax M3 语义 provider 值，统一写入 `minimax_m3`。
3. SQLite 初始化/回填迁移将已有非空 `semantic_provider_type` 也收敛到 `minimax_m3`，避免旧值继续影响后续 Todo 语义产物。
4. 新增 command 边界回归测试，验证 unsupported Todo / semantic provider 输入会被保存和回读为 `semantic_m3` / `minimax_m3`。
5. 新增数据库初始化回归测试，验证已有 unsupported semantic provider 持久化值会在初始化时写回 `minimax_m3`。
6. 本 plan 不改变 MiniMax M3 base URL、模型名和 API Key 配置能力；只收敛 provider 边界。

## 19. Plan 16：Session Command 边界抽取

2026-06-13 完成第十六个小步迁移：

1. 新增 `src-tauri/src/commands/session.rs`。
2. 将 `flush_current_session` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::session`。
3. 新增 `flush_current_session_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::session::flush_current_session`。
5. 新增单元测试验证手动刷新 command 会创建 manual session，并经 pending job 边界处理为空会话 `skipped`。
6. 测试显式将初始化 demo manual session 移出 10 秒冷却窗口，避免绕过生产冷却保护。
7. 本 plan 不拆 start / stop recording 或 simulate audio slice；录音控制继续进入后续小步迁移。

## 20. Plan 17：Recording Simulate Command 边界抽取

2026-06-13 完成第十七个小步迁移：

1. 新增 `src-tauri/src/commands/recording.rs`。
2. 将 `simulate_audio_slice` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::recording`。
3. 新增 `simulate_audio_slice_payload()` 作为不依赖 Tauri `State` 的可测试 command 业务边界。
4. `tauri::generate_handler!` 改为注册 `commands::recording::simulate_audio_slice`。
5. 新增单元测试验证模拟有效录音切片会写入 audio segment、触发 transcription processing job，并返回 runtime 刷新数据。
6. 本 plan 不拆真实 `start_recording` / `stop_recording` 的硬件线程控制；这些继续进入后续小步迁移。

## 21. Plan 18：Stop Recording Command 边界抽取

2026-06-13 完成第十八个小步迁移：

1. 将 `stop_recording` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::recording`。
2. 新增 `stop_recording_payload()` 作为可测试 command 业务边界，承接停止录音、落库 audio segment、关闭录音开关和 pending job 处理逻辑。
3. `tauri::generate_handler!` 改为注册 `commands::recording::stop_recording`。
4. 新增单元测试验证空闲状态下停止录音不会触发硬件依赖，并返回 runtime 与 latest session 刷新数据。
5. 本 plan 不拆 `start_recording` 的真实麦克风启动逻辑；启动 command 继续进入后续小步迁移。

## 22. Plan 19：Start Recording Command 边界抽取

2026-06-13 完成第十九个小步迁移：

1. 将 `start_recording` Tauri command 从 `src-tauri/src/lib.rs` 移入 `commands::recording`。
2. 新增 `start_recording_payload()` 作为可测试 command 业务边界，承接重复启动保护、真实麦克风 controller 启动和录音开关写入逻辑。
3. `tauri::generate_handler!` 改为注册 `commands::recording::start_recording`。
4. 新增单元测试验证已有 recorder 时重复开始录音不会触发硬件依赖，也不会清空现有 recorder。
5. 至此录音相关 Tauri command 已集中到 `commands::recording`；后续继续拆底层录音 controller、转写 job 和 query service。

## 23. Plan 20：Todo Domain DTO 边界抽取

2026-06-13 完成第二十个小步迁移：

1. 新增 `src-tauri/src/domain/todo.rs`。
2. 将 `TodoDto` 从 `src-tauri/src/lib.rs` 移入 `domain::todo`，作为 v0.4 Todo 领域数据契约的第一步。
3. `src-tauri/src/lib.rs` 通过 `domain::todo::TodoDto` 使用 Todo DTO，不再在根模块直接定义该结构体。
4. 新增单元测试验证 `TodoDto` 仍按 `camelCase` 序列化，保持前端 `conversationSessionId` JSON 契约不变。
5. 本 plan 不拆 Todo 查询 service、状态枚举或 TodoCandidate；这些继续进入后续 domain/query service 小步迁移。

## 24. Plan 21：Session Domain DTO 边界抽取

2026-06-14 完成第二十一个小步迁移：

1. 新增 `src-tauri/src/domain/session.rs`。
2. 将 `SessionDto` 从 `src-tauri/src/lib.rs` 移入 `domain::session`，作为会话领域数据契约的第一步。
3. `src-tauri/src/lib.rs` 通过 `domain::session::SessionDto` 使用会话 DTO，不再在根模块直接定义该结构体。
4. 新增单元测试验证 `SessionDto` 仍按 `camelCase` 序列化，保持前端 `relatedTodoIds` JSON 契约不变。
5. 本 plan 不拆 session 查询 service、会话状态枚举或转写聚合逻辑；这些继续进入后续 domain/query service 小步迁移。

## 25. Plan 22：Runtime Domain DTO 边界抽取

2026-06-14 完成第二十二个小步迁移：

1. 新增 `src-tauri/src/domain/runtime.rs`。
2. 将 `RuntimeStatusDto` 从 `src-tauri/src/lib.rs` 移入 `domain::runtime`，作为运行状态领域数据契约的第一步。
3. `src-tauri/src/lib.rs` 通过 `domain::runtime::RuntimeStatusDto` 使用运行状态 DTO，不再在根模块直接定义该结构体。
4. 新增单元测试验证 `RuntimeStatusDto` 仍按 `camelCase` 序列化，保持前端 `runtimeLabel` / `currentSessionStatus` JSON 契约不变。
5. 本 plan 不拆 runtime query service、状态枚举或前端展示；这些继续进入后续 domain/query service 小步迁移。

## 26. Plan 23：Settings Domain DTO 边界抽取

2026-06-14 完成第二十三个小步迁移：

1. 新增 `src-tauri/src/domain/settings.rs`。
2. 将 `SettingsDto` 从 `src-tauri/src/lib.rs` 移入 `domain::settings`，作为设置领域数据契约的第一步。
3. `settings_service`、`commands::settings`、`jobs::todo_extraction` 改为直接引用 `domain::settings::SettingsDto`，降低对子模块从根模块取类型的依赖。
4. 新增单元测试验证 `SettingsDto` 仍按 `camelCase` 序列化，保持前端 `recordEnabled` / `semanticProviderType` / `semanticModelName` JSON 契约不变。
5. 本 plan 不拆 settings service、provider 默认值、设置表迁移或前端设置页；这些继续进入后续 domain/service 小步迁移。

## 27. Plan 24：Model Test Domain DTO 边界抽取

2026-06-14 完成第二十四个小步迁移：

1. 新增 `src-tauri/src/domain/model_test.rs`。
2. 将 `ModelTestRequest` / `ModelTestResult` 从 `src-tauri/src/lib.rs` 移入 `domain::model_test`，作为模型连接测试 command 的数据契约边界。
3. `commands::model_test` 改为直接引用 `domain::model_test::{ModelTestRequest, ModelTestResult}`，不再通过根模块取得模型测试 DTO。
4. 新增单元测试验证模型测试请求和结果仍按 `camelCase` 序列化，保持前端 `settings.semanticProviderType` / `statusCode` JSON 契约不变。
5. 本 plan 不改变 MiniMax M3 Todo 测试占位行为、ASR 连通性测试逻辑或模型 provider 配置；这些继续按后续 provider/service 小步推进。

## 28. Plan 25：Desktop Context Domain DTO 边界抽取

2026-06-14 完成第二十五个小步迁移：

1. 新增 `src-tauri/src/domain/desktop.rs`。
2. 将 `DesktopContext` 从 `src-tauri/src/lib.rs` 移入 `domain::desktop`，作为桌面上下文 command 的前端数据契约边界。
3. `commands::desktop_context` 改为直接引用 `domain::desktop::DesktopContext`，不再通过根模块取得桌面上下文 DTO。
4. 新增单元测试验证 `DesktopContext` 仍按 `camelCase` 序列化，保持前端 `recorderStatus` JSON 契约不变。
5. 本 plan 不改变桌面上下文文案、provider 统计逻辑或前端展示；这些继续按后续 UI / command 小步推进。

## 29. Plan 26：Bootstrap Domain DTO 边界抽取

2026-06-14 完成第二十六个小步迁移：

1. 新增 `src-tauri/src/domain/bootstrap.rs`。
2. 将 `BootstrapData` 从 `src-tauri/src/lib.rs` 移入 `domain::bootstrap`，作为启动数据 command 的前端数据契约边界。
3. `commands::bootstrap` 改为直接引用 `domain::bootstrap::BootstrapData`，不再通过根模块取得启动数据 DTO。
4. 新增单元测试验证 `BootstrapData` 仍按 `camelCase` 序列化，保持前端 `settings.semanticProviderType` / `todos` / `runtime.runtimeLabel` JSON 契约不变。
5. 本 plan 不改变启动查询逻辑、demo 数据、SQLite 读取路径或前端首屏展示；这些继续按后续 app/query service 小步推进。

## 30. Plan 27：Recording Action Domain DTO 边界抽取

2026-06-14 完成第二十七个小步迁移：

1. 新增 `src-tauri/src/domain/recording.rs`。
2. 将 `RecordingActionResult` 从 `src-tauri/src/lib.rs` 移入 `domain::recording`，作为录音 command 返回前端的数据契约边界。
3. `commands::recording` 改为直接引用 `domain::recording::RecordingActionResult`，不再通过根模块取得录音动作返回 DTO。
4. 新增单元测试验证 `RecordingActionResult` 仍按 `camelCase` 序列化，保持前端 `runtime.runtimeLabel` / `latestSession` JSON 契约不变。
5. 本 plan 不改变真实录音 controller、start / stop / simulate 行为或音频落库逻辑；这些继续按后续 recording app/infra 小步推进。

## 31. Plan 28：Processing Action Domain DTO 边界抽取

2026-06-14 完成第二十八个小步迁移：

1. 新增 `src-tauri/src/domain/processing.rs`。
2. 将 `ProcessingActionResult` 从 `src-tauri/src/lib.rs` 移入 `domain::processing`，作为 pending jobs command 返回前端的数据契约边界。
3. `commands::jobs` 改为直接引用 `domain::processing::ProcessingActionResult`，不再通过根模块取得 pending jobs 返回 DTO。
4. 新增单元测试验证 `ProcessingActionResult` 仍按 `camelCase` 序列化，保持前端 `runtime.runtimeLabel` / `latestSession` / `todos` JSON 契约不变。
5. 本 plan 不改变 pending job runner、Todo 语义产物登记、runtime 查询或前端刷新行为；这些继续按后续 jobs/app/query service 小步推进。

## 32. Plan 29：Transcript Domain Record 边界抽取

2026-06-14 完成第二十九个小步迁移：

1. 新增 `src-tauri/src/domain/transcript.rs`。
2. 将 `TranscriptRecord` 从 `src-tauri/src/lib.rs` 移入 `domain::transcript`，作为转写结果进入会话聚合前的领域记录边界。
3. `src-tauri/src/lib.rs` 改为引用 `domain::transcript::TranscriptRecord`，不再在根模块直接定义该记录结构。
4. 新增单元测试验证 `TranscriptRecord` 字段契约和 `Debug` 能力保持稳定，便于后续转写 job 拆分时复用。
5. 本 plan 不改变 ASR 调用、转写任务状态、会话创建、Todo 语义产物登记或前端刷新行为；这些继续按后续 transcript job/app 小步推进。

## 33. Plan 30：Query Service 边界抽取与版本收尾

2026-06-14 完成第三十个小步迁移：

1. 新增 `src-tauri/src/app/query_service.rs`。
2. 将 Todo 列表查询、会话列表查询、最近会话查询和运行状态查询从 `src-tauri/src/lib.rs` 移入 `app::query_service`。
3. `commands::bootstrap`、`commands::jobs`、`commands::recording`、`commands::session` 改为通过 `app::query_service` 读取前端刷新数据。
4. 新增单元测试验证 query service 可读取 demo Todo、demo session 和 runtime 状态。
5. 版本号同步到 `0.4.0`：`package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`。
6. 新增 v0.4 发布说明、归档清单、验收记录和功能 review 证据。
7. 新增 Apple 风格设置页视觉参考图：`AI文档/设计参考/apple-liquid-glass-style-reference-v0.4-20260614.png`。

## 34. v0.4 收尾结论

v0.4 已完成架构地基版本收尾：

1. Provider、SQLite、domain DTO、command、settings service、query service、Todo semantic artifact 边界已建立。
2. Todo 默认入口固定为 MiniMax M3 语义边界，不再保留旧 Qwen / llama.cpp Todo runtime。
3. 设置页可见本地 ASR、本地说话人分离、云端 MiniMax M3 和预留 provider 的隐私边界。
4. 本版本仍不接入真实 WhisperKit / Argmax、SpeakerKit 或 MiniMax M3 生成链路；这些进入 v0.5 / v0.6。
5. `src-tauri/src/lib.rs` 仍保留录音线程、ASR 兜底调用和 pending job 编排，后续版本继续按风险小步抽离。
